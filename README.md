# inciando a aplicação
- clone o projeto no seu computador
- crie um ambiente virtual python venv na raiz do projeto
- inicie o ambiente virtual activate
- instale as dependencias django e django-admin utilizando pip install
- rode a aplicação django runserver
- acesse http://127.0.0.1:8000/estoque/
